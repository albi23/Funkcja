import javax.swing.*;

/**
 Write a programmer's program
 * f (x) = ax ^ 2 + bx + c, where the parameter is not equal to zero.
 */
class MainClass {
     public static void main(String[] args){

        ParametrWindow parametrWindow = new ParametrWindow();
        parametrWindow.setVisible(true);
        parametrWindow.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
    }
}
